
<%= form_for(@question) do |f| %>
  <% if @question.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@question_.errors.count, "error") %> prohibited
        this post from being saved:</h2>
    <ul>
    <% @question_.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  
  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_field :answer %>
  </div>
  <div class="actions">
   <%= submit_tag "Answer", :class=>"button" %>
  </div>
<% end %>